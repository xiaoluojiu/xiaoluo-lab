# 线上词法 Router 产物训练报告

> 脚本 `backend/scripts/router/train_runtime_l1.py`；口径 `eval_harness.py` + `l1_data.assemble_route`。
> 生成时间 2026-09-24 18:13:36；产物 D:\xiaoluodataweb\xiaoluoAIdata\xiaoluo-lab\backend\models\local_router\lexical_l1.pkl

## 验收闸门

| # | 闸门 | 结果 | 含义 |
| --- | --- | --- | --- |
| ① | 输入口径一致 | 通过 | 离线 `H.request_text` 与线上 `model.request_text` 在 971 条上逐字符相同 |
| ② | 超参同源 | 通过（构造保证） | 直接调用 `baseline_lexical.lexical_model()`，无手抄配置 |
| ③ | 线上 == 离线 | 通过 | `route_request()` 与 `assemble_route()` 在 971 条上逐条相同 |
| ④ | 反序列化一致 | 通过 | 重载后逐条预测一致，概率和最大误差 6.66e-16 |
| ⑤ | 标签空间覆盖 | 通过 | 缺失类 无 |

## 准确率（分组 5 折 CV，模板级切分）

| 口径 | route 准确率 | 用途 |
| --- | --- | --- |
| 设计口径（不含升级样本训练） | 80.2% | **产物实际训练方式** |
| 对账口径（含升级样本训练） | 80.2% | 与 `L1_EXPERIMENT_SUMMARY.md` 的 80.7% 对账 |
| shadow 口径（去掉 available_columns） | 80.3% | **线上真实水平下界** |

评测样本数 971（含升级样本 —— 它们的路线由 L0 规则给出，与线上调用顺序一致）。

## 模型

| 项 | 值 |
| --- | --- |
| 产物大小 | 2666 KB |
| 特征维数 | 8780 |
| 类别数 | 36 / 36 |
| 全量拟合耗时 | 0.159 s |
| 推理延迟 | 0.4458 ms·条⁻¹ |

## 数据指纹

| 文件 | 字节 | 行数 | sha256[:16] |
| --- | --- | --- | --- |

## 注意

- **shadow 口径的数字才代表线上**：线上路由发生时拿不到 `available_columns`（要解析 Parquet）。
  计划中的重训数据来自 shadow trace，其输入口径与此列一致 ⇒ 不要拿另一列当预期。
- 产物只保证**结构上不可能输出平台不存在的工具**（标签空间动态取自注册表）；
  平台增删工具后 `LexicalRouterModel.staleness()` 会判定过期并让 Router 返回 None（保守升级）。

## 明细

| 指标 | 词法 Router(设计口径) | 词法基线(对账口径) | 词法 Router(shadow 口径·无列) |
| --- | --- | --- | --- |
| 样本数 | 971 | 971 | 971 |
| **整条决策准确率** | 80.2% | 80.2% | 80.3% |
| 决策类型准确率 | 98.6% | 98.5% | 98.6% |
| ① 可执行决策准确率 | 79.4% | 79.4% | 79.5% |
| ② 语义闸门准确率 | 96.6% | 96.6% | 96.6% |
| 工具选择准确率 | 79.7% | 79.7% | 79.8% |
| · 类型对时工具对 | 79.4% | 79.4% | 79.5% |
| chat F1 | 89.2% | 87.9% | 89.2% |
| call F1 | 99.4% | 99.3% | 99.4% |
| ask F1 | 88.3% | 88.3% | 88.3% |
| escalate F1 | 98.2% | 98.2% | 98.2% |
| 无谓升级率 ↓ | 0.0% | 0.0% | 0.0% |
| 漏升级率 ↓ | 3.4% | 3.4% | 3.4% |
| 升级理由准确率 | 89.7% | 89.7% | 89.7% |