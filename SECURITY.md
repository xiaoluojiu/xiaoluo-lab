# 安全策略 / Security Policy

## 支持的版本

| 版本 | 是否受理漏洞报告 |
| --- | --- |
| `main` 分支当前 HEAD | ✅ 受理 |
| 更早的历史提交 | ❌ 不受理（请先在 `main` 上复现） |

本项目处于毕业设计/自部署阶段，没有对外发布版本号，因此以仓库 `main` 分支为准。

## 报告方式

**请勿在公开 Issue 中描述未修复的安全问题。** 请通过以下任一私密渠道联系维护者：

- 邮件：见仓库维护者信息（GitHub 个人主页 `xiaoluojiu`）
- GitHub Security Advisory：<https://github.com/xiaoluojiu/xiaoluo-lab/security/advisories/new>

报告时请尽量包含：

1. 影响组件（后端接口路径 / 前端页面 / 具体文件与行号）；
2. 复现步骤与所需的请求样例（请脱敏数据）；
3. 影响面评估（是否需要登录、是否需要内网、能否造成数据破坏）；
4. 若已有修复思路，一并附上。

我们目标在 **7 天内**给出首次答复，并在修复后同步告知。

## 已知的安全取舍（部署前请务必阅读）

本项目是**单租户、无鉴权**的本地数据分析平台。这是刻意的产品取舍，
但意味着在不可信网络里直接暴露端口是不安全的。已知边界：

| 项 | 现状 | 部署建议 |
| --- | --- | --- |
| 身份认证 | **没有**。所有 API 对所有能访问到端口的人开放 | 只监听内网/VPN；或在反向代理层补认证 |
| 跨站请求 | 默认不放开 CORS（`CORS_ALLOW_ORIGINS` 留空） | 确需跨域时**显式列出域名**，不要配 `*` |
| 跨域凭据 | `CORS_ALLOW_CREDENTIALS` 默认 false | 除非有 CSRF 防护，否则保持关闭 |
| 敏感配置 | LLM Key 等写在 `backend/.env`（已被 `.gitignore` 忽略） | **不要把 `.env` 提交进仓库**；Key 泄露请立即轮换 |
| 连接器口令 | 用 Fernet 对称加密落库（`app/connectors/crypto.py`） | 多实例部署必须显式注入同一个 `CONNECTOR_SECRET_KEY` |
| 日志脱敏 | 工具参数写入日志前经过 `redact()`（`app/core/logging.py`） | 新增日志点时复用同一函数，不要直接打参数字典 |
| 限流 | 仅对昂贵写端点生效，且是**进程内计数** | 多实例/对外场景请在反向代理层限流 |
| 第三方依赖 | 数量较少；唯一的密码学依赖是 `cryptography` | 定期升级；不要引入无人维护的小型包 |

## 报告 GitLab CI/Secrets 泄露

若发现仓库历史里混入了真实凭据（API Key、连接器口令）：

1. 立即在服务商后台**轮换/吊销**该凭据；
2. 再做 git history 清理；
3. 不要把泄露的 Key 贴在 Issue 里，即便已经作废。

## 致谢

我们会在修复的提交里致谢报告者；如果你不希望署名，请在报告中说明。
